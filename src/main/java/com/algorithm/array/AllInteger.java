package com.algorithm.array;

public class AllInteger {

	/**
	 * 输入数字n，按顺序输出从1最大的n位10进制数。比如输入3，
	 *  则输出1、2、3一直到最大的3位数即999。
	 * 
	 * @param n
	 */
	public void printAllInt(int n) {
       int[] all = new int[n];
       for (int i = 0; i < n; i++) {
    	   for(int j = 0; j<=i;j++) {
    		   
    	   }
       }
       
	}
}
